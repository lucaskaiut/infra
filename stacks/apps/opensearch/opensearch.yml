discovery.type: single-node
bootstrap.memory_lock: true

# Security: auth enabled, HTTP TLS disabled (Traefik handles TLS termination)
# Transport TLS uses demo certificates generated by DISABLE_INSTALL_DEMO_CONFIG=false
plugins.security.ssl.http.enabled: false
