services:
  app:
    image: local/financeiro-apolar-api:latest
    env_file:
      - .env
    environment:
      CONTAINER_ROLE: web
    networks:
      - edge
      - shared_data
    volumes:
      - financeiro_apolar_storage:/var/www/html/storage/app/private
    stop_grace_period: 45s
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/up || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 120s
    deploy:
      mode: replicated
      replicas: 2
      update_config:
        order: start-first
        parallelism: 1
        delay: 15s
        failure_action: rollback
      restart_policy:
        condition: on-failure
        delay: 5s
      labels:
        - traefik.enable=true
        - "traefik.http.routers.financeiro-apolar-api.rule=Host(`financeiro-apolar-api.${DOMAIN:?defina DOMAIN no .env da stack}`)"
        - traefik.http.routers.financeiro-apolar-api.entrypoints=websecure
        - traefik.http.routers.financeiro-apolar-api.tls=true
        - traefik.http.routers.financeiro-apolar-api.tls.certresolver=letsencrypt
        - traefik.http.services.financeiro-apolar-api.loadbalancer.server.port=80
        - traefik.http.services.financeiro-apolar-api.loadbalancer.healthcheck.path=/up
        - traefik.http.services.financeiro-apolar-api.loadbalancer.healthcheck.interval=5s
        - traefik.http.services.financeiro-apolar-api.loadbalancer.healthcheck.timeout=4s

  worker:
    image: local/financeiro-apolar-api:latest
    env_file:
      - .env
    environment:
      CONTAINER_ROLE: worker
    networks:
      - shared_data
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 10s
      placement:
        constraints:
          - node.role == manager

networks:
  edge:
    external: true
    name: infra_edge
  shared_data:
    external: true
    name: infra_shared

volumes:
  financeiro_apolar_storage:
